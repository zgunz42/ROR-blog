    <!-- Mini Post -->
    <article class="mini-post">
      <header>
        <%= content_tag :h3, link_to(item_mini.title, item_mini)%>
        <%= time_tag item_mini.created_at, pubdate: true, class: 'published' %>
        <a href="#" class="author"><%= image_tag  item_mini.avatar %></a>
      </header>
      <a href="#" class="image"><%= image_tag item_mini.image %></a>
    </article>