<article>
<%= @post.title %>
  <div>
    <%= @post.content %>
  </div>
  <%= link_to 'Edit', edit_post_path(@post) %>
  <%= link_to 'Delete', post_path(@post), method: :delete, data: {confirm: "are you crazy?"} %>
</article>