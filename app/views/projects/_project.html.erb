<h3><%= project.title %></h3>
<p><%= project.description %></p>
<% unless project.missions.empty? %>
<ul>
  <% project.missions.each do |m| %>
    <li>
      <%= m.title %>
      <%= m.complete? %>
    </li>
  <% end %>
<% end %>
</ul>